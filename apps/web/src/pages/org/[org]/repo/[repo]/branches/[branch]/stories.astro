---
import Layout from '@/layouts/layout.astro'
import { RepoStoriesApp } from '@/components/apps/repo-stories-app'

export const prerender = false

const { org, repo } = Astro.params
---

<Layout title={`${org}/${repo}`}>
  <RepoStoriesApp orgName={org!} repoName={repo!} client:load />
</Layout>
