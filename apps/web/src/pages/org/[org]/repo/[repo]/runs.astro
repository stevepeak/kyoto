---
import Layout from '@/layouts/layout.astro'
import { RepoRunsApp } from '@/components/apps/repo-runs-app'

export const prerender = false

const { org, repo } = Astro.params
---

<Layout title={`${org}/${repo} runs`}>
  <RepoRunsApp orgName={org!} repoName={repo!} client:load />
</Layout>
