---
import Layout from '@/layouts/layout.astro'
import { StoryCreateApp } from '@/components/apps/story-create-app'

export const prerender = false

const { org, repo } = Astro.params
---

<Layout title={`New Story - ${org}/${repo}`}>
  <StoryCreateApp orgName={org!} repoName={repo!} client:load />
</Layout>
