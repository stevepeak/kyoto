{
  "name": "@app/context7",
  "type": "module",
  "version": "0.0.1",
  "description": "Context7 API integration tools",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "clean": "rm -rf node_modules dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0 --cache --concurrency=2",
    "lint:fix": "eslint . --fix --cache --concurrency=2",
    "lint:ci": "eslint . --max-warnings=0 --cache --concurrency=1 --no-error-on-unmatched-pattern"
  },
  "dependencies": {
    "@app/config": "workspace:*",
    "@trigger.dev/sdk": "catalog:",
    "ai": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "^22.18.10",
    "tsconfig": "workspace:*",
    "typescript": "^5.9.3"
  }
}
