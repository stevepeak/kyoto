{
  "name": "@app/daytona",
  "type": "module",
  "version": "0.0.1",
  "description": "Daytona sandbox integration utilities and tools",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "clean": "rm -rf node_modules dist",
    "typecheck": "tsc --noEmit",
    "deploy": "daytona snapshots create kyoto-cli-base:0.0.1 --dockerfile ./Dockerfile",
    "lint": "eslint . && prettier --ignore-path='../../.prettierignore' --check .",
    "fix": "eslint --fix --quiet . && prettier --ignore-path='../../.prettierignore' --log-level=silent --write ."
  },
  "dependencies": {
    "@app/config": "workspace:*",
    "@daytonaio/sdk": "catalog:",
    "@opentelemetry/api": "catalog:",
    "@trigger.dev/sdk": "catalog:",
    "ai": "catalog:",
    "ts-dedent": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@kyoto/eslint-config": "workspace:*",
    "@types/node": "catalog:",
    "tsconfig": "workspace:*",
    "typescript": "catalog:"
  }
}
