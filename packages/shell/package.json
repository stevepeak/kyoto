{
  "name": "@app/shell",
  "type": "module",
  "version": "0.0.1",
  "description": "Shell tools for file system and terminal operations",
  "main": "dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0 --cache --concurrency=2",
    "lint:fix": "eslint . --fix --cache --concurrency=2",
    "lint:ci": "eslint . --max-warnings=0 --cache --concurrency=1 --no-error-on-unmatched-pattern"
  },
  "dependencies": {
    "ai": "^5.0.107",
    "chalk": "^5.6.2",
    "execa": "^9.6.1",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^22.18.10",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "ora": "^9.0.0"
  }
}

